import { PagePlaceholder } from "@/shared/components/layout/page-placeholder";

export function TransactionsPage() {
  return (
    <PagePlaceholder
      title="Lançamentos"
      description="A estrutura global desta página já suporta tabelas, filtros e ações futuras. Os dados e regras de negócio ainda serão adicionados."
    />
  );
}
