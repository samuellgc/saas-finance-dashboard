import { PagePlaceholder } from "@/shared/components/layout/page-placeholder";

export function ContactsPage() {
  return (
    <PagePlaceholder
      title="Contatos"
      description="O shell autenticado desta seção já está pronto. Listagens, filtros e formulários de contato serão desenvolvidos depois."
    />
  );
}
